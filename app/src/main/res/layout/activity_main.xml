<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    >

    <RadioGroup
    android:id="@+id/tab_host"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:background="@color/white"
    android:layout_alignParentBottom="true"
    android:gravity="center"
    android:orientation="horizontal" >

    <RadioButton
    android:id="@+id/tab_index"
    style="@style/tabhost"
    android:drawableTop="@drawable/icon_tab_index"
    android:text="首页" />

    <RadioButton
    android:id="@+id/tab_route"
    style="@style/tabhost"
    android:drawableTop="@drawable/icon_tab_route"
    android:text="路线" />

    <RadioButton
    android:id="@+id/tab_setting"
    style="@style/tabhost"
    android:drawableTop="@drawable/icon_tab_more"
    android:text="更多" />
    </RadioGroup>

    <View
        android:id="@+id/view_div"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="#10000000"
        android:layout_above="@id/tab_host"
        />
    
    <FrameLayout
        android:id="@+id/fl_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/view_div"

        ></FrameLayout>
    
</RelativeLayout>
